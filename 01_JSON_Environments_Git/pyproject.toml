[project]
name = "01-json-environments-git"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Arid-P", email = "aridamanpatel.srijan@gmail.com" }
]
requires-python = ">=3.14"
dependencies = [
    "requests>=2.34.2",
    "rich>=15.0.0",
]

[project.scripts]
01-json-environments-git = "01_json_environments_git:main"

[build-system]
requires = ["uv_build>=0.12.17,<0.13.0"]
build-backend = "uv_build"
